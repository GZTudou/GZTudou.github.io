body{
	color:#222;
}
p,strong{
	margin:0;
}
ol,ul{
	margin:15px 0;
}

/* 标题列表 日期样式 */
.posts-collapse .post-meta{
	position: absolute;
    font-size: 14px;
    left: 20px;
    top: 3px;
}

/* 标题列表样式 */
.post-header h1 a span {
   
    color: #000;
    margin-left: 10px;
    font-style: inherit;
}

Inherited from a.post-title-link

/* 标题列表 上方时间 样式 2016 */
.posts-collapse .collection-title h2 {
    margin-bottom: -30px;
	
}

.posts-expand .post-title-link {
    color: currentColor;
	 font-size: 20px;
}

/*coderlmm 样式*/
.site-header .site-title a{
	color:#111;
}

